name: CI

on:
    pull_request:

jobs:
    run_kurtosis:
        runs-on: ubuntu-latest
        steps:
            - name: Run Kurtosis
              uses: kurtosis-tech/kurtosis-github-action@v1
              with:
                path: "."